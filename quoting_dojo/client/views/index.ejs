<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title>Quoting Dojo!</title>
            <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">
    </head>
    <body>
        <div class="container">
            <div class="the_form col-sm-4 col-sm-offset-4">
                <h1>Share your favorite quote!</h1>
                <% if(typeof(errors) !='undefined') {%>
                    <% for (var x in errors) {%>
                        <h3><%= errors[x].message %></h3>
                    <%} %>
                <% } %>

                <form class="" action="/quotes" method="post">
                    <label>Name:</label>
                    <input type="text" name="name" placeholder="Name"></br>
                    <label>Quote:</label>
                    <textarea name="quote" rows="3" cols="45"></textarea></br>
                    <button type="submit" class="btn btn-default pull-left" formmethod="post" name="button">Add to quotes!</button>
                    <button type="submit" class="btn btn-default pull-right" formmethod="get" name="button">Skip to quotes</button>
                </form>
        </div>
            <!-- <button type="button" action="/quotes" id="skip" name="button">Skip to quotes</button> -->
    </div>
    </body>
</html>
