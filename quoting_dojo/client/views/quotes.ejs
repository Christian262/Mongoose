<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title>Quotes</title>
        <% if(typeof(errors) !='undefined') {%>
            <% for (var x in errors) {%>
                <h3><%= errors[x].message %></h3>
            <%} %>
        <% } %>
    </head>
    <body>
        
            <div class="quotes col-sm-4 col-sm-offset-4">
                <h1>Here are the awesome quotes!</h1>

                <% for (var x in quotes) {%>
                    <p>"<%= quotes[x].quote %>"</p>
                    <p>-<%= quotes[x].name  %> at <%= quotes[x].createdAt %></p>
                    <hr />
                <%} %>

                <a href="/" class="button">Return to home page</a>
        </div>


    </body>
</html>
