<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title>Edit</title>
        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">
    </head>
    <body>
        <div class="container">
            <h1>Edit <%= dinosaurs.name %></h1>
            <!-- <% if(typeof(errors) !='undefined') {%>
                <% for (var x in errors) {%>
                    <h3><%= errors[x].message %></h3>
                <%} %>
            <% } %> -->
            <form  action="/<%= dinosaurs.id %>" method="post">
                <label>Name:</label>
                <input type="text" name="name" value="<%= dinosaurs.name %>"><br/>
                <label>Cool thing:</label>
                <input type="text" name="cool" value="<%= dinosaurs.cool %>"><br/>
                <label>Why would you have it as a pet:</label><br/>
                <textarea name="pet" rows="5" cols="50" value="<%= dinosaurs.pet %>"></textarea>
                <button type="submit" name="submit">Edit <%= dinosaurs.name %></button>
            </form>
            <a href="/">Home again!</a>
        </div>
    </body>
</html>
